{
  "$id": "app.config.schema.json",
  "description": "App configuration",
  "required": ["pbHost", "language"],
  "properties": {
    "pbHost": {
      "type": "string"
    },
    "language": {
      "type": "string",
      "enum": ["en", "pl"]
    }
  }
}
